DIAGNÓSTICO desde DevTools Console del usuario:

:3333/api/conversations:1  Failed to load resource: 404 (Not Found)
conversations.js:36 [convs] refresh failed SyntaxError: Unexpected token '<', "<!DOCTYPE "... is not valid JSON

PROBLEMA CRÍTICO: Los endpoints REST nuevos del Bloque 8.2 (GET /api/conversations, POST /api/conversations, GET /api/conversations/:id/messages, PATCH /api/conversations/:id) NO están registrados en server.ts en runtime, aunque el código los define.

El servidor está devolviendo el HTML del index.html para esas rutas (de ahí el SyntaxError parseando "<!DOCTYPE" como JSON).

INVESTIGA:
1. Lee src/web/server.ts ACTUAL y verifica que los handlers app.get('/api/conversations'), app.post(...) están definidos
2. Verifica que se registran ANTES del catch-all express.static() o del routing genérico
3. Si están definidos pero no funcionan, ¿se reinició el servidor tras los cambios? La sesión de shinobi_web.cmd actual puede tener código viejo cargado en memoria

ADEMÁS:
- El bug del logo enso (rectángulo rojo sin mask-image) probablemente también es de carga: index.html no encuentra el CSS o el path al PNG
- El bug del botón enviar es porque app.js falla al cargar las conversaciones inicialmente → JS muere → no se bindea ningún listener

ORDEN DE FIX:
1. Verifica que server.ts tiene los endpoints REST registrados correctamente
2. Mata cualquier proceso node activo (shinobi_web.cmd previo en memoria)
3. Arranca shinobi_web.cmd fresco
4. Verifica que GET /api/conversations devuelve JSON [] en lugar de HTML
5. Verifica que /api/conversations/:id/messages funciona
6. Después aborda los bugs visuales (logo enso + marca de agua) que probablemente se resuelven cuando el JS no muere al cargar

Muéstrame qué encuentras en server.ts antes de tocar nada.