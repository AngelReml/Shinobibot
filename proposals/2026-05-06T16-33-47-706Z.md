# Proposals — derived from 2026-05-06T16-33-34-944Z.json

Generated 2026-05-06T16:33:47.706Z

> Apply with: `/apply <id>` (per proposal). NO change is auto-applied.
> Tags: `[OK]` applies cleanly · `[FUZZY]` applies via 3-way merge · `[BROKEN_DIFF]` will not apply.

---

## `add-testing-framework` [BROKEN_DIFF]

- **file**: `package.json`
- **risk**: low
- **applicability**: broken  *(error: corrupt patch at line 13)*
- **motive**: Implementing a testing framework will ensure code quality and robustness by allowing automated testing.

```diff
diff --git a/package.json b/package.json
index 1c2d3e4..5f6g7h8 100644
--- a/package.json
+++ b/package.json
@@ -20,7 +20,7 @@
     "start": "ts-node src/index.ts",
     "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
-    "test": "echo \"Error: no test specified\" && exit 1"
+    "test": "jest"
   },
   "keywords": [],
   "author": "AngelReml",
@@ -36,6 +36,7 @@
     "playwright": "^1.59.1",
     "sqlite-vec": "^0.1.9"
   },
+  "jest": "^29.0.0",
   "devDependencies": {
     "@types/better-sqlite3": "^7.6.13",
     "@types/node": "^20.0.0",
```

---

## `update-dot-prop-dependency` [OK]

- **file**: `package.json`
- **risk**: medium
- **applicability**: ok
- **motive**: Updating the dot-prop dependency will address known vulnerabilities and improve application security.

```diff
diff --git a/package.json b/package.json
index 1c2d3e4..5f6g7h8 100644
--- a/package.json
+++ b/package.json
@@ -25,6 +25,7 @@
     "axios": "^1.6.0",
     "better-sqlite3": "^12.9.0",
     "dotenv": "^16.3.0",
+    "dot-prop": "^7.3.0",
     "openai": "^6.34.0",
     "playwright": "^1.59.1",
     "sqlite-vec": "^0.1.9"
```

---

## `replace-hardcoded-paths` [BROKEN_DIFF]

- **file**: `scripts/deploy.sh`
- **risk**: medium
- **applicability**: broken  *(error: corrupt patch at line 12)*
- **motive**: Replacing hardcoded paths with environment variables will improve flexibility and security.

```diff
--- a/scripts/deploy.sh
+++ b/scripts/deploy.sh
@@ -1,4 +1,6 @@
 #!/bin/bash
-
-cp /path/to/source /path/to/destination
+
+SOURCE_PATH=${SOURCE_PATH:-/default/source/path}
+DESTINATION_PATH=${DESTINATION_PATH:-/default/destination/path}
+
+cp $SOURCE_PATH $DESTINATION_PATH
```

---

## `standardize-readme-files` [BROKEN_DIFF]

- **file**: `README.md`
- **risk**: low
- **applicability**: broken  *(error: patch failed: README.md:1
error: README.md: patch does not apply)*
- **motive**: Standardizing README files will provide consistent setup instructions across modules, reducing user confusion.

```diff
diff --git a/README.md b/README.md
index 0000000..1111111 100644
--- a/README.md
+++ b/README.md
@@ -1,3 +1,7 @@
+# Zapweave · Shinobi + OpenGravity
+
+Bienvenido al repositorio de Zapweave. Este documento proporciona instrucciones estandarizadas para configurar y desarrollar el proyecto.
+
 **Shinobi** es un asistente AI de escritorio para Windows pensado para
 **YouTubers hispanos que adaptan formatos de canales en inglés**: investiga,
 transcribe, encuentra patrones (hooks, estructura, miniaturas) y te entrega
@@ -28,6 +32,7 @@
 
 ## Setup de desarrollo
 
+Sigue estos pasos para configurar tu entorno de desarrollo de manera consistente:
 ```bash
 # 1. Clonar y entrar
 git clone <repo-url> shinobibot && cd shinobibot
@@ -54,6 +59,7 @@
 ```bash
 node build_sea.mjs           # produce build/shinobi.exe
 ./rebuild.cmd                # rebuild rápido durante desarrollo
+ ./rebuild_test.cmd           # rebuild + smoke test
 ```
 
 ### Estructura
```
