# Proposals — derived from 2026-05-06T19-29-02-787Z.json

Generated 2026-05-06T19:33:02.679Z

> Apply with: `/apply <id>` (per proposal). NO change is auto-applied.
> Tags: `[OK]` applies cleanly · `[FUZZY]` applies via 3-way merge · `[BROKEN_DIFF]` will not apply.

---

## `patch-dot-prop` [BROKEN_DIFF]

- **file**: `package.json`
- **risk**: low
- **applicability**: broken  *(error: patch fragment without header at line 12: @@ -3,5 +3,5 @@)*
- **motive**: Addresses high-risk vulnerability in external dependency 'dot-prop' by updating to a patched version.

```diff
--- a/package.json
+++ b/package.json
@@ -1,5 +1,5 @@
 {
   "name": "shinobibot",
   "version": "1.0.0",
-  "dot-prop": "4.2.1",
+  "dot-prop": "5.3.0",
   "type": "module",
   "main": "dist/index.js",
   "scripts": {
@@ -3,5 +3,5 @@
     "test": "vitest"
   },
   "license": "MIT, ISC"
-}
+}
```

---

## `fix-hardcoded-paths` [OK]

- **file**: `scripts/a4_run.ts`
- **risk**: low
- **applicability**: ok
- **motive**: Ensures test scripts run on non-Windows systems and for different users by replacing hardcoded Windows paths with dynamic OS paths.

```diff
diff --git a/scripts/a4_run.ts b/scripts/a4_run.ts
--- a/scripts/a4_run.ts
+++ b/scripts/a4_run.ts
@@ -2,11 +2,12 @@
 // Vuelca los reports crudos en docs/A4_VALIDATION.md sin evaluar.
 import * as fs from 'fs';
 import * as path from 'path';
+import * as os from 'os';
 import { runRead } from '../src/reader/cli.js';
 
 const TARGETS = [
-  { label: 'opengravity', name: 'OpenGravity', path: 'C:\\Users\\angel\\Desktop\\OpenGravity' },
-  { label: 'execa', name: 'execa', path: 'C:\\Users\\angel\\Desktop\\test_repos\\execa' },
+  { label: 'opengravity', name: 'OpenGravity', path: path.join(os.homedir(), 'Desktop', 'OpenGravity') },
+  { label: 'execa', name: 'execa', path: path.join(os.homedir(), 'Desktop', 'test_repos', 'execa') },
 ];
 
 async function main() {
```

---

## `add-unit-tests` [OK]

- **file**: `src/runner.test.ts`
- **risk**: low
- **applicability**: ok
- **motive**: Stabilizes critical system interactions by implementing unit tests for the core runner module.

```diff
--- /dev/null
+++ b/src/runner.test.ts
@@ -0,0 +1,10 @@
+import { describe, it, expect } from 'vitest';
+import { runner } from './runner';
+
+describe('runner', () => {
+  it('should initialize without errors', () => {
+    expect(() => runner.initialize()).not.toThrow();
+  });
+
+  it('should handle valid mission paths', () => {
+    expect(runner.validatePath('/valid/path')).toBe(true);
+  });
+});
```

---

## `remove-dead-code` [BROKEN_DIFF]

- **file**: `src/kernel.ts`
- **risk**: medium
- **applicability**: broken  *(error: src/kernel.ts: No such file or directory)*
- **motive**: Reduces cognitive load and maintenance burden by removing unused 'LoopDetector' logic from the kernel.

```diff
--- a/src/kernel.ts
+++ b/src/kernel.ts
@@ -1,10 +1,3 @@
 import { MissionRuntime } from './mission_runtime';
 
-export class LoopDetector {
-  constructor() {}
-  detectCycle(): boolean {
-    return false;
-  }
-}
-
 export class Kernel {
   private runtime: MissionRuntime;
```

---

## `standardize-license` [BROKEN_DIFF]

- **file**: `package.json`
- **risk**: low
- **applicability**: broken  *(error: corrupt patch at line 9)*
- **motive**: Resolves conflicting license declarations (MIT vs ISC) in package.json for legal clarity and consistency.

```diff
--- a/package.json
+++ b/package.json
@@ -1,5 +1,5 @@
 {
   "name": "shinobibot",
   "version": "1.0.0",
   "dot-prop": "5.3.0",
   "type": "module",
   "main": "dist/index.js",
   "scripts": {
     "test": "vitest"
   },
-  "license": "MIT, ISC"
+  "license": "MIT"
 }
```
