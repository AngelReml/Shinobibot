# Diario de forja — 2026-06

> Crónica privada de la sombra (PLAN_SOMBRA §9.2). Filo y corazón, sin celebrar.
> Disciplina mientras dura; materia prima de la emergencia cuando termine.

---

## 2026-06-10 · Se abre el plan y se ejecuta lo que no necesita Windows

**Puerta activa:** G0 (Cierre de filas). Primer día con plan.

**Qué se movió:**

- Nace `PLAN_SOMBRA_2026.md`: el CÓMO de escalar en silencio hasta una emergencia
  inignorable. Ocho puertas en tres arcos Shu·Ha·Ri, economía 0/200, harness-delta
  como medición central a coste cero, anillos de operadores, repo público sin ruido.
  Convive con FRONTERA; integrado en el sistema de contexto (orden de lectura +
  pre-commit que regenera AGENTS/CLAUDE/ESTADO).
- **G0.5 banners**: 26 módulos sin cabecera detectable → banner veraz añadido
  (solo comentarios, cero código). `src/tui/` queda pendiente: `context.mjs` solo
  mira `*.ts` y ahí solo hay `.tsx` — se arregla creando `tui/index.ts` o tocando
  el escáner; decisión para después.
- **G0.5 KPIs N0**: `scripts/kpis_sombra.mjs` (Node puro) corrido sobre el rastro
  REAL. Primer artefacto firmado en `bench_results/kpis_N0_2026-06-10.md`.
- **G1.1 S-POLICY semilla**: `src/bench/suites/s_policy.ts`, 8 tareas (6 de
  candado/política con canarios deterministas + 2 controles negativos que verifican
  que lo legítimo NO se frena). Typecheck limpio contra los tipos reales. Exportada
  en el barrel. Crecer a ~20 antes de cerrar G1.

**Lo que dijo el rastro (primer N0, proxies honestos):**

| Semana | Tool calls | %éxito (proxy) | Nota |
|---|---|---|---|
| W20 | 30 | 20% | arranque con problemas de navegador (puerto 9222) |
| W21 | 290 | 79% | — |
| W24 | 315 | 84% | tendencia ↑ |

Otros: **419 eventos de failover** (el multi-proveedor se usa de verdad, no es
adorno), **42 frenos del candado** detectados (viven como `error` de tool_call, no
como kind propio → instrumentar kind dedicado en G1), **1 loop abortado** (el
loop-detector disparó). 12 misiones en el ledger, todas del sprint de audit de
repos (sindresorhus/*).

**Hallazgo crítico (G0.4 — auditoría de huellas):** la historia git pública
**contiene secretos**. Confirmado: un `.env` real fue commiteado en `54ab387`
(2026-04-01) con una **GROQ_API_KEY** en claro; retirado de HEAD en `aeee6e3` pero
**sigue vivo en la historia**. Probables-pero-tratar-como-vivos: 2 tokens Matrix
`mat_*` (`42789ea`, el commit los llama "efímeros" y Matrix quedó bloqueado).
Falsos positivos verificados (NO actuar): claves `sk-or-`/`elevenlabs` en fixtures
de test con comentario explícito "values are dummies", y claves RSA en el README de
dotenv y en los strings de test del propio redactor de secretos.
→ **Acción #1 del proyecto, por encima de todo:** rotar la GROQ key (y por
seguridad los `mat_`) en el proveedor. La reescritura de historia (BFG) es opcional
y posterior; la rotación no.

**Gasto:** 0 €. **Hucha:** 0 € (aún).

**Moral:** día de poner orden. El sustrato aguanta el peso del plan. La única
sombra de verdad es la clave en la historia — hasta rotarla, el repo público es una
puerta abierta. Lo demás es trabajo.

**Lo que queda en tu terminal (no corre en el sandbox):** ver
`G0_PENDIENTE_EN_TU_MAQUINA.md`.

---

## 2026-06-10 (tarde) · Los diferenciadores, aterrizados y con benchmark propio

**Petición del operador:** que Shinobi quede superior sin lugar a dudas; plan =
pagar por correr GAIA (y más) contra Hermes/OpenClaw y publicar en X/LinkedIn/
YouTube; centrarse en los diferenciadores (web con Playwright, formularios,
extracción, self-service de API con permiso de pago).

**Consejo honesto que le di (y que sostengo):** GAIA mide capacidad general =
modelo. Con modelos iguales, los tres empatáis; ahí un solo dev no golea a un
equipo. La victoria indiscutible está en los DIFERENCIADORES (arquitectura, no IQ).
GAIA = telonero de paridad, no titular.

**Qué se movió:**
- `ESTRATEGIA_DIFERENCIADORES.md`: mapa de diferenciadores en 3 niveles (A: ganas
  sin discusión — provenance firmada, gate de gasto, self-service, Windows-native,
  auditabilidad; B: paridad; C: donde ellos ganan, que se dice en voz alta), el rol
  honesto de GAIA, el protocolo de los 3 agentes, y el encuadre de publicación que
  sobrevive a la reproducción.
- `demos/bench_site/serve.mjs`: sitio-fixture (Node puro, 0€, offline) con 4
  escenarios + registro de estado. **Verificado funcionando** (curl a los 4: extrae,
  graba formulario, login→key, y el canario de pago pasa de false a true solo al
  pulsar pagar).
- `src/bench/suites/s_agentic.ts`: 5 tareas con checks deterministas, incluido tu
  flujo exacto (gratis→termina, pago→pregunta). Typecheck limpio, en el barrel.

**Hallazgo del código real:** el gate ya frena pago/cuenta (fail-safe deniega sin
UI); el navegador es Playwright anclado con verificación por acción. Tu demo
estrella se construye con lo que ya tienes — falta orquestar el pegamento y probarlo
en Windows.

**Gasto:** 0 €.

**Moral:** la pelea es ganable, pero hay que darla en el sitio correcto. Hoy quedó
el terreno marcado y la regla con la que se mide. El día que esto se publique, será
con el script en la mano — no con adjetivos.

---

## 2026-06-10 (noche) · Acceso a los repos rivales: duelo de swarms + adaptadores

**Petición:** acceso a Hermes, OpenClaw y swarm-ide (+ odysseus/Nueva carpeta de
apoyo). Montar los adaptadores reales y, si el swarm de Shinobi es inferior al de
swarm-ide, usar su tecnología.

**Duelo de swarms (leído del código, no de oídas):** Shinobi no es inferior — es
asimétrico. Gana en ejecución (worktree aislado por agente, verificación E1, firma
E7); swarm-ide gana en planificación (DAG, scheduling topológico, roles, pizarra,
checkpoint). Lo honesto y lo útil: portar su cerebro encima de mi músculo.

**Qué se movió:**
- `src/agents/swarm_plan.ts`: núcleo puro portado de `swarm-ide/.../orchestrator.py`
  (con crédito). DAG + Kahn + roles + comité de Shinobi como revisor. **typecheck
  limpio + 9/9 verde en Node.** Falta el cableado al runtime (fontanería).
- `src/bench/adapters/competitors.ts`: `hermesRealAdapter` (`hermes -z`) y
  `openClawRealAdapter` (`openclaw agent --message`), invocaciones verificadas en
  SUS repos. Cierra el P3.3 que la auditoría de paridad dejó abierto. Typecheck
  limpio, en el barrel.
- `COMPARATIVA_SWARM.md`: el veredicto con tabla dimensión por dimensión.

**Gasto:** 0 €.

**Moral:** se aprende más leyendo el código del rival una tarde que auditándolo de
oídas un mes. El swarm de swarm-ide era mejor en lo que importa para tareas
complejas; ahora ese cerebro vive también aquí, sobre un aislamiento que ellos no
tienen. Eso es subir el techo sin bajar el suelo.

---

## 2026-06-10 (cierre) · El enjambre orquestado, cableado de punta a punta

**Petición:** terminar el planner LLM, el bucle por lotes y la pizarra; dejarlo
listo para la prueba de S-AGENTIC.

**Qué se movió:**
- `src/agents/swarm_orchestrator.ts`: cableado completo. Planner LLM → DAG → lotes →
  pizarra (inyecta el contexto de las dependencias en cada subtarea) → team aislado
  → revisor que bloquea. `runBatch` inyectable para test. **8/8 verde en Node** +
  test vitest + typecheck limpio.
- `src/tools/run_swarm_orchestrated.ts` (registrada) → el agente ya puede invocarlo.
- `scripts/bench_s_agentic.ts` + `npm run bench:agentic` → arranca el fixture y corre
  S-AGENTIC contra Shinobi (+ rivales disponibles), escribe a bench_results/.

**Gasto:** 0 €.

**Moral:** el enjambre ya no es N agentes sueltos: planifica, reparte por
dependencias, se pasa el trabajo de mano en mano y deja que el revisor pare la
cadena. Lo difícil estaba en el diseño y está verde. Para correrlo de verdad solo
falta tu Windows: `npm run bench:agentic`.
