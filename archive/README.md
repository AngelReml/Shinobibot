# archive/

Scripts one-off de validación de sprints históricos (sprint1_1 … sprintV6,
audit_validation). **No se referencian desde `src/`** y están excluidos del
typecheck (`tsconfig.json → exclude`). Se conservan por valor histórico;
si necesitas espacio, son borrables — git conserva la historia.

Movidos aquí el 2026-06-06 (ver DOSSIER_AUDITORIA_2026-06-06.md §4).
