// EXTIRPADO (Fase 2, 2026-06-12) — tool cloud eliminada (gateway externo).
// Este stub existe solo para que tools/index.ts compile sin cambios de path.
// La tool no se registra: no hay registerTool() call.
export {};
